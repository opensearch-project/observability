receivers:
  otlp:
    protocols:
      grpc:
      http:
exporters:
  file:
    path: /etc/volume/log.json
  jaeger:
    endpoint: jaeger:14250
    tls:
      insecure: true
service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [file, jaeger]
